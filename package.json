{
  "name": "tweet-discord-share",
  "version": "0.6.29",
  "private": true,
  "description": "Share X/Twitter posts to Discord via webhooks (Tampermonkey / Violentmonkey userscript).",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check-dist.mjs && node --check dist/tweet-discord-share.user.js",
    "test": "node --test tests/*.test.mjs"
  }
}
