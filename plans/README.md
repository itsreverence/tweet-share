# Implementation Plans

Generated by the improve skill on 2026-07-18. Execute in the order below unless
dependencies say otherwise. Each executor must read the selected plan fully,
honor its STOP conditions, and update its status when finished.

This checkout was at local commit `b8c520f` when the plan was written, while the
public implementation baseline is v0.6.23 commit `24720a2`. Plan 001 therefore
requires a baseline ancestry check and explicitly forbids an executor from
syncing or rewriting the operator's branch without authorization.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Restore compact, reliable Discord media delivery | P1 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- Plan 001 has no plan dependency, but implementation must begin from v0.6.23
  (`24720a2`) or a descendant so the newer onboarding/settings work is retained.

## Findings considered and rejected

- Recreate the old v0.6.22 implementation verbatim: rejected because partial
  resolution can omit skipped video links, and preview overstates attachment
  eligibility.
- Restore the old “Upload media to Discord” preference: rejected because the
  selected direction preserves v0.6.23's simpler settings and makes bounded
  upload with reliable fallback the default delivery policy.
- Remove the visible tweet permalink: rejected because it remains useful for
  Discord search, copying, and clients that ignore custom embed URLs.
- Perform media downloads while opening preview: rejected because it adds delay
  and bandwidth before the user chooses a destination; preview should describe
  conditional delivery honestly instead.
